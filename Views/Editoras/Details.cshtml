@model BookSphere.Models.Editora

@{
    ViewData["Title"] = "Detalhes da Editora";
}

<h2>@ViewData["Title"]</h2>
<hr />

<dl class="row">
    <dt class="col-sm-2">Nome</dt>
    <dd class="col-sm-10">@Model.Nome</dd>
</dl>

<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning me-2" aria-label="Editar Editora">
        <i class="fa fa-edit"></i> Editar
    </a>
    <a asp-action="Index" class="btn btn-secondary" aria-label="Voltar à lista de editoras">Voltar</a>
</div>
