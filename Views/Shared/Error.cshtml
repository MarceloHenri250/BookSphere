@model ErrorViewModel
@{
    ViewData["Title"] = "Erro";
}

<div class="container mt-5">
    <div class="text-center">
        <h1 class="text-danger">Erro</h1>
        <h2 class="text-danger">Ocorreu um erro ao processar sua solicitação.</h2>

        @if (Model.ShowRequestId)
        {
            <p>
                <strong>ID da solicitação:</strong> <code>@Model.RequestId</code>
            </p>
        }

        <h3>Modo de Desenvolvimento</h3>
        <p>
            Alterando para o ambiente <strong>Desenvolvimento</strong>, será exibida mais informação detalhada sobre o erro que ocorreu.
        </p>
        <p>
            <strong>O ambiente de Desenvolvimento não deve ser habilitado em aplicações de produção.</strong>
            Isso pode resultar na exibição de informações sensíveis de exceções para os usuários finais.
        </p>
        <p>
            Para depuração local, habilite o ambiente <strong>Desenvolvimento</strong> definindo a variável de ambiente <strong>ASPNETCORE_ENVIRONMENT</strong> como <strong>Development</strong>
            e reiniciando o aplicativo.
        </p>

        <div class="mt-4">
            <a class="btn btn-warning" href="@Url.Action("Index", "Home")">Voltar à Página Inicial</a>
        </div>
    </div>
</div>
